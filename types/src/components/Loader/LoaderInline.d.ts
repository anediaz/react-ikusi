import * as React from 'react';
import './loader-inline.css';
interface LoaderInlineProps {
    height?: number;
    width?: number;
    color?: string;
    opacity?: string;
}
export declare const LoaderInline: ({ width, height, color, opacity }: LoaderInlineProps) => React.JSX.Element;
export {};
